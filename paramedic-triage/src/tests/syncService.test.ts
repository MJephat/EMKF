describe("SyncService", () => {

    it("uploads pending records", () => {
        
    });

    it("marks records as synced", () => {

    });

    it("does not upload already synced records", () => {

    });

    it("keeps record pending when upload fails", () => {

    });

});